# V5 Offline 修改与验证记录

目标版本：Minecraft Java 26.1.2；验证平台：Windows x86_64、JDK 25.0.3。安装与联网边界见 [OFFLINE.md](OFFLINE.md)。

## 修改范围

| 原有路径 | 本版本处理 |
| --- | --- |
| V5Loader 远程认证、解密、更新与迁移 | 删除实现和启动入口；直接加载本地代码 |
| 模块自动获取与额外 JAR 加载 | 删除下载和自动加载；脚本手动安装，缺失依赖仅提示 |
| Mojang 会话、访问令牌与账号档案 | 删除相关读取、认证请求和 mixin；本地玩家 API 只读取世界实体 |
| V5 后端、IRC、WebSocket、远程命令 | 删除实现、模块入口及依赖 |
| Discord RPC、Webhook、截图与封禁上报 | 删除实现；通知和错误留在本地 |
| 远程图片、音乐下载及外部进程控制台 | 删除联网实现；保留本地资源和游戏日志 |
| 公开物品及市场数据 | 仅允许两个固定 Hypixel 公共 GET，支持本地缓存 |
| 寻路原生库 | 从 NativeSrc 重新编译 Windows DLL，成品排除其他平台旧 V5 原生库 |

同时修复了运行中发现的兼容问题：清除已删除 WelcomeScreen 的脚本全局声明；避免重复初始化配置文件；让嵌套安排的客户端任务正常执行；公共数据刷新避免当前 Rhino 引擎存在问题的循环解构和可选链写法。

## 验证结果

- 使用本地依赖缓存执行 `clean build --offline --no-daemon`（与隔离客户端运行任务一起执行），构建成功。
- 静态解析全部 181 个 JavaScript 文件，检查 885 条本地导入声明；没有缺失文件、命名导出或外部模块依赖。修改后的脚本通过 Prettier 格式检查。
- 检查最终 JAR 的启动元数据、脚本全局声明、删除的类、敏感联网标记、嵌入依赖和 DLL；确认 DLL 与本地源码构建产物相同，且可以加载并执行 `initNative`。
- 对 HTTP 入口检查 13 种禁止地址，包括第三方域名、Mojang、HTTP、伪造域名、查询参数、重定向相关地址变体、用户信息、非公开 API 和本地文件地址；均在连接前拒绝。
- 设置会抛错的 JVM 全局代理和认证器后，两个公共请求仍成功；检查私有客户端无 CookieHandler、无认证器、不使用代理、不跟随重定向。
- 实际启动 Minecraft 客户端：注册 71 个模块，打开 V5 GUI，截图确认界面，执行脚本重载后再次打开界面。联网测试取得 5424 个按名称索引的物品条目和 2197 个 Bazaar 产品（数量以测试时返回值为准）。
- 通过仅作用于测试 JVM 的主机解析文件，将 Hypixel 及原版账号服务映射至本机不可用地址，验证公共请求失败后的缓存回退；同时验证首次无缓存启动、价格查询、GUI 和重载。没有修改系统 hosts 文件。
- 两个仓库均通过 `git diff --check`。

验证辅助代码、完整日志和截图保存在工作区根目录 `.AI_work/`，不装入最终 Mod 或脚本包。构建和启动过程中发现的问题均经过修改后复测；目录中也保留了诊断阶段日志，应以名称含 `final` 的最新断网测试日志为准。

## 检查边界

已检查当前仓库中的加载入口、联网/会话读取/进程执行路径、脚本依赖，以及嵌入第三方库的相关入口和网络调用。发现并删除了读取访问令牌、远程控制、数据上报等不符合本次要求的代码路径。第三方通用库仍可能包含通用网络类；这不等于它们在本版本启动时会调用第三方服务，也不构成对任意后加代码的隔离。

未登录真实账号、未连接 Hypixel 实测所有宏和多人玩法；启动验证使用开发环境的测试身份。原版 Realms 的测试身份认证失败以及可选其他 Mod 的 mixin 缺失警告，不属于 V5 账号联网。构建缓存来源于公开依赖仓库，未对全部第三方依赖进行形式化验证。

V5 自身不读取或附带账号令牌、用户名、UUID、Cookie、API Key 等身份字段到公共 HTTP 请求。保留的 Hypixel 官方 Mod API 使用游戏连接订阅并接收位置事件。Minecraft 本体、其他 Mod、后续手动加入的 Java 互操作脚本及网络源 IP 均不能由此描述为匿名或完全断网。网络接口与限制详见安装说明。

这份记录说明检查范围与实测结果，不声称可以证明任意未来脚本、依赖或所有运行环境中绝无恶意行为。
